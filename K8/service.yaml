kind: Service                             
apiVersion: v1
metadata:
  name: kanban-service
spec:
  ports:
    - port: 80                               
      targetPort: 8000                
  selector:
    app: kanban                   
  type: NodePort
